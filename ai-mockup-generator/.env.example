# Get your API key from https://aistudio.google.com/apikey
# Note: The app stores the API key in localStorage, so this file is optional.
# You can also configure the API key directly in the app's settings.
VITE_GEMINI_API_KEY=your_api_key_here
